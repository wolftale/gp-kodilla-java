package com.kodilla.good.patterns.product_ordering;

import java.time.LocalDateTime;

public class ProductOrderRepository implements OrderRepository {
    public void createProductOrder(User user, String productName, int quantity, LocalDateTime orderDate) {
    }

    public void createOrder(User user, String productName, int quantity, LocalDateTime orderDate) {
    }
}
